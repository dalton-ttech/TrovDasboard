/* Independent advertising reports and Shopify-only observations. */
(() => {
  'use strict';
  function adsOverview({ snapshot, period }, H) {
    const { icon, escape: e, pageHeading, stamp } = H;
    const current = snapshot?.periods?.[period];
    const ready = snapshot?.status === 'ready' && current?.status === 'ready';
    const range = ready ? `${current.since.slice(5).replace('-', '.')} – ${current.until.slice(5).replace('-', '.')}` : `近 ${period} 天`;
    const control = `<label class="period-control">${icon('calendar')}<span>${e(range)}</span><select id="ads-period" aria-label="销量总览统计区间（太平洋时间）"><option value="30" ${period === '30' ? 'selected' : ''}>近 30 天</option><option value="7" ${period === '7' ? 'selected' : ''}>近 7 天</option></select>${icon('down')}</label>`;
    function metric(key, label, source, decimals, prefix = '', suffix = '') {
      const value = ready ? current[key] : null;
      const available = Number.isFinite(value);
      const formatted = available ? new Intl.NumberFormat('en-US', { minimumFractionDigits: decimals, maximumFractionDigits: decimals }).format(value) : '—';
      const direction = available ? current.comparison?.directions?.[key] : null;
      const trendLabel = direction === 'up' ? '较前一日同长度区间提升' : direction === 'down' ? '较前一日同长度区间下降' : direction === 'flat' ? '与前一日同长度区间持平' : '';
      const arrow = ['up', 'down'].includes(direction) ? `<sup class="ads-trend ${direction}" title="${trendLabel}"><svg viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M6 10V2M2.5 5.5 6 2l3.5 3.5"/></svg></sup>` : '';
      return `<div class="ads-kpi"><span class="ads-kpi-label">${label}</span><strong class="ads-kpi-value"><span class="sr-only">${e(prefix + formatted + suffix)}${trendLabel ? '，' + trendLabel : ''}</span><span class="ads-kpi-number" aria-hidden="true">${prefix ? `<small>${prefix}</small>` : ''}<span class="ads-digit-stack"><span class="ads-count-size">${formatted}</span><span class="ads-count" ${available ? `data-count-target="${value}" data-count-decimals="${decimals}"` : ''}>${formatted}</span></span>${suffix ? `<small>${suffix}</small>` : ''}${arrow}</span></strong><span class="ads-kpi-source">${source}</span></div>`;
    }
    return `<section class="ads-overview" aria-label="销量总览">${pageHeading('SALES OVERVIEW', '销量总览', snapshot?.syncedAt ? '上次读取 ' + e(stamp(snapshot.syncedAt)) : '上次读取 —', control)}<div class="ads-kpis">${metric('shopifyNetSales', '销售额', 'Shopify 净销售额', 2, '$')}${metric('shopifyOrders', '订单量', 'Shopify 有效订单', 0, '', '单')}${metric('metaRoas', 'ROAS', 'Meta · A02 / A03', 2, '', '×')}</div></section>`;
  }
  const period = r => r.since === r.until ? r.since.replaceAll('-', '.') : r.since.replaceAll('-', '.') + ' — ' + r.until.slice(5).replace('-', '.');
  const kindName = kind => kind === 'weekly' ? '周报' : '日报';
  function reportFeature(r, H, primary) {
    if (!r) return '';
    const { escape: e, icon, money, number } = H;
    return `<article class="report-feature ${primary ? 'featured-daily' : 'featured-weekly'}"><div class="report-feature-top"><span class="report-label">${icon(primary ? 'file' : 'chart')}最新${kindName(r.kind)}</span><span class="report-edition">${primary ? 'DAILY BRIEF' : 'WEEKLY REVIEW'}</span></div><a class="report-feature-title" href="#report/${e(r.id)}"><span>${e(period(r))}</span>${icon('arrow')}</a><div class="report-scope">Meta × Shopify <span>·</span> ${e(r.adScope.join(' / '))}</div><p class="report-summary">${e(r.summary || '打开报告查看本期投流数据与分析。')}</p><div class="report-metrics"><div><span>广告花费</span><strong>${money(r.spend)}</strong><small>USD · Meta</small></div><div><span>Meta 购买</span><strong>${number(r.metaPurchases, 0)}<small>次</small></strong><small>平台归因</small></div><div><span>Meta ROAS</span><strong>${number(r.metaRoas, 2)}<small>×</small></strong><small>购买价值 / 花费</small></div></div><div class="report-feature-bottom"><span>Shopify 本投放订单 <strong>${number(r.campaignOrders, 0)}</strong><small>UTM</small></span><a class="text-button" href="#report/${e(r.id)}">阅读${kindName(r.kind)} ${icon('arrow')}</a></div></article>`;
  }
  function reportArchive(reports, kind, query, H) {
    const { escape: e, icon, money, number } = H;
    const needle = query.trim().toLowerCase();
    const list = reports.filter(r => (kind === 'all' || r.kind === kind) && (!needle || `${r.since} ${r.until} ${r.title} ${r.summary} ${kindName(r.kind)}`.toLowerCase().includes(needle)));
    if (!list.length) return `<div class="report-empty">${icon('search')}<h3>没有匹配的报告</h3><p>试试日期，例如 2026-09，或切换日报 / 周报。</p></div>`;
    return `<div class="report-archive-head"><span>报告 / 统计区间</span><span>广告花费</span><span>Meta 购买</span><span>Shopify 本投放</span><span></span></div>${list.map(r => `<a class="report-archive-row" href="#report/${e(r.id)}" aria-label="阅读 ${e(period(r))} ${kindName(r.kind)}"><span class="report-row-name"><span class="report-kind ${r.kind}">${kindName(r.kind)}</span><span><strong>${e(period(r))}</strong><small>${e(r.title)}</small></span></span><span class="report-row-stat"><small>花费</small><strong>${money(r.spend)}</strong></span><span class="report-row-stat"><small>Meta 购买</small><strong>${number(r.metaPurchases, 0)}<em> 次</em></strong></span><span class="report-row-stat"><small>本投放订单</small><strong>${number(r.campaignOrders, 0)}<em> 单 · UTM</em></strong></span><span class="report-row-arrow">${icon('chevron')}</span></a>`).join('')}<div class="report-count">共 ${list.length} 份报告 · 金额为 USD · 按统计区间结束日期排序</div>`;
  }
  function reportsPage({ catalog, kind, query, reportId, serverAvailable }, H) {
    const { icon, escape: e, pageHeading, stamp } = H;
    const reports = catalog.reports || [];
    if (reportId) {
      const r = reports.find(item => item.id === reportId);
      if (!r) return `${pageHeading('REPORT LIBRARY', '报告暂不可用', '该报告尚未完成，或已不在当前归档中。')}<a class="button secondary" href="#reports">返回报告中心 ${icon('arrow')}</a>`;
      return `<div class="report-reader-heading"><a class="text-button back-link" href="#reports">${icon('arrow')}全部报告</a><div class="reader-title-row"><div><div class="eyebrow">${r.kind === 'weekly' ? 'WEEKLY REVIEW' : 'DAILY BRIEF'} / ${e(r.adScope.join(' + '))}</div><h1>${e(r.title)}</h1><p>${e(period(r))} <span>· America/Los_Angeles</span></p></div><div class="reader-actions"><a class="button secondary" href="${e(r.files.html)}" target="_blank" rel="noopener">独立阅读 ${icon('external')}</a></div></div><div class="report-reader-meta"><span>${icon('checkCircle')}已完成 · Trov_ADS 原始报告</span><span>生成于 ${stamp(r.generatedAt)}</span></div></div><div class="report-document-wrap"><iframe class="report-document" src="${e(r.files.html)}" title="${e(r.title)} ${e(period(r))}" sandbox="allow-same-origin"></iframe></div><p class="page-footnote">${icon('info')}本页保留原始报告的结论与统计口径。Meta 平台归因购买与 Shopify 的 UTM 订单分别呈现。</p>`;
    }
    const daily = reports.filter(r => r.kind === 'daily'), weekly = reports.filter(r => r.kind === 'weekly');
    return `${pageHeading('ADVERTISING INTELLIGENCE', '投流报告', '每日复盘，每周看清方向。')}<div class="report-section-intro"><span><i class="small-dot blue"></i>A02 / A03 <span class="report-intro-divider">/</span> Meta × Shopify</span>${serverAvailable ? `<button class="text-button" data-refresh-reports>${icon('refresh')}刷新归档</button>` : `<span class="source-label">${reports.length ? '最近发布的报告快照' : '报告尚未发布'}</span>`}</div><div class="report-feature-grid">${reportFeature(daily[0], H, true)}${reportFeature(weekly[0], H, false)}</div><div class="report-attribution-note">${icon('info')}Meta 购买是平台归因结果；Shopify 本投放订单按报告内的 UTM 口径识别，两者独立对照。</div><section class="report-library"><div class="report-library-heading"><div><h2>报告归档</h2><p>${daily.length} 份日报 <span>·</span> ${weekly.length} 份周报</p></div><label class="search-field">${icon('search')}<input id="report-search" type="search" placeholder="搜索日期或报告内容" aria-label="搜索报告" value="${e(query)}" autocomplete="off"></label></div><div class="report-tabs" role="group" aria-label="报告类型">${[['all','全部报告'],['daily','日报'],['weekly','周报']].map(([id,label]) => `<button class="${kind === id ? 'selected' : ''}" aria-pressed="${kind === id}" data-report-kind="${id}">${label}<span>${id === 'all' ? reports.length : id === 'daily' ? daily.length : weekly.length}</span></button>`).join('')}</div><div id="report-archive" aria-live="polite">${reportArchive(reports, kind, query, H)}</div></section><p class="page-footnote">${icon('archive')}${serverAvailable ? '沿用 Trov_ADS 已完成的报告与模板；刷新归档会读取新增文件。' : '当前展示最近发布的报告快照；新报告发布后更新。'}日报、周报按各自区间展示，不跨周期相加。</p>`;
  }
  function dataPage({ sync, history, rows, syncing, reports, serverAvailable }, H) {
    const { pageHeading, sectionHeading, icon, escape: e, stamp } = H;
    const n = sync?.shipments.length || 0, cov = sync?.coverage || {};
    const fields = [['下单时间','orderCreatedAt','Shopify Order'],['发货信息建立时间','fulfillmentCreatedAt','Fulfillment createdAt'],['进入运输时间','inTransitAt','Shopify 观测'],['实际送达时间','deliveredAt','仅已有时间的包裹'],['预计送达时间','estimatedDeliveryAt','Shopify 预计时间'],['物流事件','events','Fulfillment 级事件'],['最近观测位置','latestLocation','最新有位置的事件']];
    const conflicts = rows.filter(s => s.dataConflict);
    return `${pageHeading('DATA & SOURCES', '数据与来源', '每个状态、每个时间，都有清晰的出处。')}<div class="data-grid connected-data-grid"><section class="panel connection-card"><div class="source-heading"><span class="source-symbol">${icon('bag')}</span><div><h2>Shopify</h2><p>订单 · 发货 · 物流事件</p></div><span class="badge ${sync ? 'delivered' : 'pending'}"><i></i>${sync ? '已读取' : serverAvailable ? '待首次读取' : '待发布'}</span></div><div class="source-stats"><div><strong>${n}</strong><span>已载入运单</span></div><div><strong>${sync?.historicalMatches ?? '—'}<small> / ${history.shipments.length}</small></strong><span>历史运单匹配</span></div></div><dl class="source-details"><div><dt>上次成功读取</dt><dd>${stamp(sync?.syncedAt)}</dd></div><div><dt>订单查询区间 · UTC</dt><dd>${sync ? e(sync.window.since) + ' 至 ' + e(sync.window.untilExclusive) + '（不含）' : '—'}</dd></div><div><dt>接入方式</dt><dd>${serverAvailable ? 'Trov_ADS 现有 API · 只读' : '已发布数据快照 · 只读'}</dd></div></dl><div class="connection-card-bottom"><span>${icon('shield')}${serverAvailable ? '保留上次成功快照' : sync ? '显示最近发布的快照' : '等待首份快照发布'}</span>${serverAvailable ? `<button class="button primary" data-sync-shopify ${syncing ? 'disabled aria-busy="true"' : ''}>${icon('refresh', syncing ? 'spinning' : '')}${syncing ? '正在读取…' : '刷新 Shopify'}</button>` : ''}</div></section><section class="panel"><div class="source-heading"><span class="source-symbol">${icon('archive')}</span><div><h2>历史 OMS</h2><p>历史表单.xlsx · 原始字段</p></div><span class="source-label">历史快照</span></div><div class="source-stats"><div><strong>${history.shipments.length}</strong><span>历史运单</span></div><div><strong>${history.shipments.filter(s => s.shippedAt).length}</strong><span>有 OMS 发货时间</span></div></div><dl class="source-details"><div><dt>快照日期</dt><dd>${e(history.snapshotDate)}</dd></div><div><dt>补充字段</dt><dd>仓内时间、运费、服务类型、偏远程度</dd></div><div><dt>关联方式</dt><dd>运单号精确匹配</dd></div></dl><p class="source-note">运输状态优先采用 Shopify 的观测结果，原始 OMS 信息独立保留。</p></section><section class="panel coverage-panel">${sectionHeading('Shopify 字段覆盖', '按全部已载入运单计算；缺失值不参与时效计算', '<span class="source-label">'+n+' 票</span>')}<div>${fields.map(([label,key,note]) => `<div class="coverage-line"><span><strong>${label}</strong><small>${note}</small></span><span class="coverage-meter"><i style="width:${n ? (cov[key] || 0)/n*100 : 0}%"></i></span><strong>${cov[key] || 0}<small> / ${n}</small></strong><span class="coverage-percentage">${n ? Math.round((cov[key] || 0)/n*100) : 0}%</span></div>`).join('')}</div><div class="panel-note">${icon('info')}送达字段未覆盖可能表示尚未送达，也可能表示源数据尚未提供。</div></section><div class="data-side"><section class="panel source-review">${sectionHeading('状态核对', '历史记录与当前确认分开保留')}<div class="review-count"><strong>${conflicts.length}</strong><span>票待核实</span></div>${conflicts.length ? conflicts.map(s => `<button class="source-conflict-row" data-detail="${e(s.id)}"><span><strong>${e(s.order)}</strong><small>OMS 已送达 · Shopify ${e(s.displayStatus)}</small></span>${icon('arrow')}</button>`).join('') : '<p class="source-note">当前没有发现历史送达状态与 Shopify 确认不一致。</p>'}<p class="source-note">已履约不等于已送达，冲突包裹单独标注。</p></section><section class="panel">${sectionHeading('投流报告来源', 'Trov_ADS · 已完成的日报 / 周报')}<div class="report-source-count"><strong>${reports.reports?.length || 0}</strong><span>份报告已归档</span><a href="#reports" class="text-button">浏览报告 ${icon('arrow')}</a></div><p class="source-note">独立使用 America/Los_Angeles 时区，沿用原报告的投流归因口径。</p></section></div></div><p class="page-footnote">${icon('shield')}${serverAvailable ? 'Shopify 访问凭据由本地服务使用。当前仅查询数据；页面展示最近一次成功读取的状态与时间。' : '页面展示最近发布的数据快照，保留实际读取时间；新快照发布后更新。'}</p>`;
  }
  function deliveryAnalytics({ rows }, H) {
    const { MODEL, percentile, number, sectionHeading, icon, stateNames, escape: e } = H;
    const samples = MODEL.samples(rows, 'fulfillmentCreatedAt', 'deliveredAt');
    const transit = MODEL.samples(rows, 'inTransitAt', 'deliveredAt');
    const customer = MODEL.samples(rows, 'orderCreatedAt', 'deliveredAt');
    const dueRows = rows.filter(s => s.isLive && MODEL.delivered(s) && s.deliveredAt && s.estimatedDeliveryAt && Number.isFinite(Date.parse(s.estimatedDeliveryAt)));
    const ontime = dueRows.filter(s => Date.parse(s.deliveredAt) <= Date.parse(s.estimatedDeliveryAt)).length;
    const states = [...new Set(rows.map(s => s.state).filter(Boolean))].map(state => { const group = rows.filter(s => s.state === state); const d = MODEL.samples(group,'fulfillmentCreatedAt','deliveredAt'); return {state,n:d.length,p50:percentile(d,.5),p90:percentile(d,.9)}; }).sort((a,b) => b.n-a.n || a.state.localeCompare(b.state));
    const distribution = [[0,3,'≤ 3 天'],[3,5,'3–5 天'],[5,7,'5–7 天'],[7,Infinity,'> 7 天']].map(([lo,hi,label],i) => ({label,n:samples.filter(v => (i===0?v>=lo:v>lo) && v<=hi).length}));
    const max = Math.max(1,...distribution.map(d=>d.n));
    return `<section class="analytics-metrics">${[.5,.75,.9].map(p => `<div><span class="metric-label">送达周期 P${p*100}</span><strong>${number(percentile(samples,p))}<small>天</small></strong><span class="metric-foot">Fulfillment → Delivery · n=${samples.length}</span></div>`).join('')}</section><div class="analytics-grid"><section class="panel">${sectionHeading('送达周期分布', '发货信息建立 → 已确认送达')}<div class="duration-distribution">${distribution.map(d => `<div><span>${d.label}</span><div><i style="width:${d.n/max*100}%"></i></div><strong>${d.n}<small>票</small></strong></div>`).join('')}</div><div class="panel-note">${icon('info')}来自 Shopify 有效时间戳，按实际经过时间计算</div></section><section class="panel">${sectionHeading('不同视角的时效', '各指标使用自己的有效样本')}<div class="delivery-facts"><div><span>观测运输中位数<small>进入运输 → 送达 · n=${transit.length}</small></span><strong>${number(percentile(transit,.5))}<small>天</small></strong></div><div><span>客户等待中位数<small>下单 → 送达 · n=${customer.length}</small></span><strong>${number(percentile(customer,.5))}<small>天</small></strong></div><div><span>按当前预计时间准时送达<small>送达 ≤ 当前预计时间 · n=${dueRows.length}</small></span><strong>${number(dueRows.length ? ontime/dueRows.length*100 : null)}<small>%</small></strong></div></div><div class="panel-note">${icon('info')}预计送达可能调整；该比率不代表原始承诺达成率</div></section><section class="panel state-panel delivery-state-panel">${sectionHeading('目的州时效', 'Fulfillment → Delivery · 天 · 5 票起展示州级基线')}<div class="state-head"><span>目的州</span><span>有效样本</span><span>P50</span><span>P90</span></div>${states.map(g => `<button class="state-row" data-state-link="${e(g.state)}"><span><strong>${e(g.state)}</strong><small>${e(stateNames[g.state])}</small></span><span>${g.n}<small>票</small></span><span>${g.n>=5 ? number(g.p50) : '<span class="low-sample">样本不足</span>'}</span><span>${g.n>=5 ? number(g.p90) : '—'}</span></button>`).join('')}</section></div>`;
  }
  function liveDetail({ shipment: s, sync, alert }, H) {
    const { escape: e, icon, stamp, statusBadge, money, number, MODEL, stateNames } = H;
    const labels = {CONFIRMED:'发货信息已确认',IN_TRANSIT:'进入运输',OUT_FOR_DELIVERY:'正在派送',DELIVERED:'已送达',ATTEMPTED_DELIVERY:'尝试派送',FAILURE:'配送异常',DELAYED:'发生延误',READY_FOR_PICKUP:'可供提取',LABEL_PRINTED:'运单已打印',LABEL_PURCHASED:'已购买运单',CARRIER_PICKED_UP:'承运商已揽收'};
    const location = s.latestLocation ? [s.latestLocation.city,s.latestLocation.state].filter(Boolean).join(', ') : '—';
    const timeline = [{status:'ORDER',title:'订单创建',happenedAt:s.orderCreatedAt},{status:'FULFILLMENT',title:'发货信息建立',happenedAt:s.fulfillmentCreatedAt},...(s.events||[])].filter(event=>event.happenedAt).sort((a,b)=>Date.parse(a.happenedAt)-Date.parse(b.happenedAt));
    const line = event => `<li class="timeline-entry"><span class="timeline-node">${icon('check')}</span><div class="timeline-entry-content"><strong>${e(event.title || labels[event.status] || event.status)}</strong><time>${stamp(event.happenedAt)}</time><p>${e([event.city,event.province].filter(Boolean).join(', ') || (event.status==='ORDER'?'Shopify Order':event.status==='FULFILLMENT'?'Shopify Fulfillment':'Shopify Fulfillment Event'))}</p></div></li>`;
    const field = (label,value) => `<div><dt>${label}</dt><dd>${e(value ?? '—')}</dd></div>`;
    return `<div class="detail-backdrop"></div><aside class="detail-panel live-detail" role="dialog" aria-modal="true" aria-labelledby="detail-title"><div class="detail-top"><span class="eyebrow">SHIPMENT DETAILS</span><button class="icon-button close-detail" aria-label="关闭包裹详情">${icon('close')}</button></div><div class="detail-header"><span class="detail-package-icon">${icon('box')}</span><div><h2 id="detail-title">订单 ${e(s.order)}</h2><span>FedEx · Shopify 观测</span></div>${statusBadge(s)}</div><div class="tracking-copy"><span><small>TRACKING NUMBER</small><strong>${e(s.id)}</strong></span><button class="icon-button" id="copy-tracking" aria-label="复制运单号">${icon('copy')}</button></div>${['CRITICAL','WARNING','REVIEW'].includes(alert.level) ? `<div class="detail-alert ${alert.level==='REVIEW'?'review-alert':''}">${icon(alert.level==='REVIEW'?'info':'warning')}<div><strong>${alert.level==='REVIEW'?'状态待核实':'需要关注'}</strong><p>${e(alert.reason)}。</p>${s.dataConflict ? `<p>Shopify：${e(s.displayStatus)} · 历史 OMS：已送达。当前不将此包裹计为已确认送达，也不累计其运输年龄。</p>` : ''}</div></div>` : ''}<section class="detail-section"><h3>配送信息</h3><div class="live-destination"><span>${icon('pin')}目的地</span><strong>${e(s.state || '—')} <small>${e(s.zip || '—')}</small></strong><p>${e(stateNames[s.state] || '目的州暂缺')}</p></div><dl class="detail-fields">${field('预计送达',stamp(s.estimatedDeliveryAt))}${field('最近观测位置',location)}${field('最近事件时间',stamp(s.lastEventAt))}${field('Shopify 状态',s.displayStatus)}${field('历史 OMS 运费',money(s.cost))}${field('历史物流服务',s.service ? s.service.replace('FEDEX_','').replaceAll('_',' ') : null)}${field('偏远程度 · 历史 OMS',s.remote==='否'?'非偏远':s.remote)}</dl><p class="detail-caption">位置来自 Shopify 物流事件，不代表实时 GPS。上次读取：${stamp(sync.syncedAt)}。</p></section><section class="detail-section timeline-section"><div class="section-heading"><h3>包裹时间线</h3><span class="source-label">Shopify · UTC</span></div><ol class="timeline">${timeline.map(line).join('')}</ol><p class="detail-caption">${s.trackingCount>1 ? `该 Fulfillment 关联 ${s.trackingCount} 个运单，物流事件为该 Fulfillment 共享记录。` : '物流事件来自对应的 Shopify Fulfillment。'}发货信息建立时间不等于承运商揽收时间。</p></section><section class="detail-section"><h3>时间效率</h3><div class="duration-list">${[['下单 → 发货信息建立','orderCreatedAt','fulfillmentCreatedAt'],['发货信息建立 → 送达','fulfillmentCreatedAt','deliveredAt'],['进入运输 → 送达','inTransitAt','deliveredAt']].map(([label,from,to])=>`<div><span>${label}</span><strong>${number(MODEL.duration(s,from,to))}<small> 天</small></strong></div>`).join('')}</div><p class="detail-caption">仅计算完整且有效的时间戳；缺失项显示为 —。</p></section><details class="detail-history"><summary>原始 OMS 与读取记录 ${icon('down')}</summary><dl class="detail-fields">${field('历史 OMS 状态',s.historyStatus)}${field('OMS 创建',s.createdAt)}${field('OMS 拣货',s.pickedAt)}${field('OMS 发货',s.shippedAt)}${field('本系统首次看到运单',stamp(s.trackingFirstSeenAt))}</dl><p class="detail-caption">OMS 时间按源表原样保留，时区未确认。系统首次看到运单的时间是读取记录，不是物流扫描时间。</p></details><div class="detail-bottom"><a class="button primary" href="https://www.fedex.com/fedextrack/?trknbr=${encodeURIComponent(s.id)}" target="_blank" rel="noopener noreferrer">在 FedEx 官网查询 ${icon('external')}</a><p>承运商网站可用于进一步核实状态。</p></div></aside>`;
  }
  window.TrovWorkspace = { adsOverview, reportsPage, reportArchive, dataPage, deliveryAnalytics, liveDetail };
})();
